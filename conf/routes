# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /persons/:personId          controllers.PersonController.personById(personId: String)
POST    /persons                    controllers.PersonController.echo